# 已退役的 workflow（tombstone）

放这里的文件**不得再发起**。留档只为可追溯，不是可用入口。

- `plan-gate-direction.js.retired` —— Plan-Gate 方向关 3 视角互盲图化版。
  2026-08-05 用户裁定简化 Plan-Gate 为「1 个 gpt-5.6-sol ultra、subagent 形式、四维单审」后退役。
  退役理由：判定过严、成本过高（4 个 ultra 进程 + 两段人工接力），且遗留三项未修的
  fail-open / 目录竞争待办。现行形态见 `~/.claude/ops/plan-gate.md`。
