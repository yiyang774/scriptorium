# Plan-Gate 分诊留痕 · 2026-08-05 规则体系修订

- **被审对象**:CLAUDE.md + ops/plan-gate.md + ops/enforcement.md + hooks/guard-*.sh 的 8 项改动
- **审查**:codex `gpt-5.6-sol` `ultra` `-s read-only`,subagent 形式单审(即本次新形态的首次自用)
- **原始输出**:`/tmp/plangate-rules-20260805-183337/gate/out.txt`(114 行)
- **裁决**:不通过 → 逐条分诊后修复 → 下方留痕

## 采纳并已修复(7 条,全部经 L0 亲自实测复现)

| # | 意见 | 复现证据 | 处置 |
|---|---|---|---|
| 1 | `guard_gc` 的 `-maxdepth 1` 含深度 0,根目录满 7 天会连 `triggers.tsv` 一起删 | `find` 实测列出根目录本身 | 加 `-mindepth 1`;回归用例入自测 |
| 2 | 只读判定在 segment 判 review、却回整串 `$CMD` 找 `-s read-only` → fail-OPEN | 把只读标志放在无关的前置段、真正的审查段不带,实测返回 0(反例原文见自测用例"只读伪装绕过应拦") | 改逐段独立验证;两条回归用例入自测 |
| 3 | 承诺闸正则误拦正常表达 | 实测误拦"我会建议保留现状"(命中"建")、"我把修改写完了"(过去完成)、"我来做个总结" | 排除"建议/了/完毕/已经",动词改完整词,去掉"我把/我来";三条回归用例入自测 |
| 4 | 记账把任意工具当"已动手",Read 一圈也算 | —— | 只读工具(Read/Grep/Glob/Web*)不记账;回归用例入自测 |
| 5 | 旧图化版仅文本写"不要调用",注册未删 | `find` 实测 `workflows/plan-gate-direction.js` 仍在 | 移入 `workflows/_retired/` 并加 tombstone README |
| 6 | 豁免语义过宽:"就按这个思路去写"不等于知情同意跳过审查 | —— | 改为从严解释,明列"批准方案 ≠ 豁免审查",拿不准就问 |
| 7 | 豁免后普通 L3 不覆盖原四维,且已有沉没成本 | —— | 新增"扩展 L3"要求(喂用户原话+spec+plan+产出,明令覆盖四维),并禁止声称与前置审等价 |

## 采纳并已修复(2 条,审查自身的 fail-open)

| # | 意见 | 处置 |
|---|---|---|
| 8 | 审查者看不到用户原话就判不出"把 A 当成 B",脚本却只要 `spec.txt` | 新增 **`request.txt` 必填非空**校验,装用户原话/约束/非目标;实测缺失即阻断 |
| 9 | 三闸只证输出非空+末行哨兵,不证四维都审了;"无则只回无致命项"会让空审通过 | GUARD 改为强制四个固定标题;新增**第四闸**机械校验四维齐全。实测:笼统一句→阻断、只答两维→阻断、四维齐全→放行 |

## 驳回(1 条,经实测证伪)

| # | 意见 | 驳回理由(一手证据) |
|---|---|---|
| 10 | 「`ultra` 不是 `model_reasoning_effort` 枚举,命令会失败或被忽略」 | **实测证伪**:`~/.codex/config.toml:147` 明确 `enabled-reasoning-efforts = [..., "ultra", "max"]`;且用 `-c model_reasoning_effort="ultra"` 实跑 codex 返回正常("OK",15,339 tokens)。**本次审查自身就是用 ultra 跑的**。属误报,按 §二·2 误报阀门写明理由后放行 |

## 存疑但不阻断(2 条,记录待观察)

| # | 意见 | L0 裁定 |
|---|---|---|
| 11 | 单审相比互盲多视角,独立发现能力确实下降;"异构性不变"表述不够准确 | **部分认同**:异构性(相对 L0 家族)确实不变,但**审查独立性与错误相关性确实变了**。这是用户明示裁定的成本/覆盖取舍,不推翻;已在锚点处注明"变的只是并发度与关卡数"——措辞保留,但此处记录该表述的局限 |
| 12 | 外层 Claude subagent 需 Bash 才能调 codex,与 `[GRANT-PERMISSIONS]` 的"审查者只读"冲突 | **已知边界,不阻断**:`ops/plan-gate.md` 已显式登记该边界与缓解措施(节点不做判断、只组装回传)。彻底修复需重设计节点权限模型,登记为独立待办 |

## 本次修复自身又引出的一个洞(已修)

| # | 症状 | 处置 |
|---|---|---|
| 13 | 逐段只读判定改完后**误拦了写留痕文件的命令**——`cat > xxx.md <<'EOF'` 的 heredoc 正文里含"Plan-Gate"字样,被当成审查调用 | 收窄关键词匹配:要求该段以 `codex exec` 开头**且**关键词出现在命令参数而非 heredoc 正文;回归用例入自测 |

## 验证

- `guard-selftest.sh`:**PASS=52 FAIL=0**(改前 37,新增 8 承诺闸 + 7 回归)
- Plan-Gate 脚本实跑(桩 codex):四维齐全→放行、笼统→阻断、缺维→阻断、缺 request→阻断、截断/空/仅哨兵→阻断
