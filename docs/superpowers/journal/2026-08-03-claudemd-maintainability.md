# 任务日志：CLAUDE.md 可维护性修复（编号漂移 / L2 外置 / 角色解耦）
状态：已交付
最后更新：2026-08-03

## 📍 当前状态（每次更新覆写此节 —— 断片后先读这里）
- 进行到：**已交付并同步生效**（10 文件哈希校验一致，实文件验收 7/7 过）
- 下一步：无（三条既有缺陷已登记 ops/plan-gate.md 待办区，另立项）
- 未决问题：无（三类范围与四项裁定已由用户确认）
- 关键路径文件：
  - `~/.claude/docs/superpowers/specs/2026-08-03-claudemd-maintainability-design.md`
  - `~/.claude/CLAUDE.md`（139 行）
  - `~/.claude/ops/{plan-gate,fable5,pr-merge,line-count,journal}.md`
  - 基线快照 `~/.claude-band-snapshots/claudemd-fix-20260803-090524`（39 文件）
- 已定裁决（仍生效的约束，一行一条）：
  - 范围仅三类问题，纯规范文本 —— 编排改进（worktree/后台/模板/hook/脚本）另立项，避免一次吃太大
  - 角色名与模型解耦（"Opus"→"L0 主循环"）—— 规则约束角色不约束模型，换模型不失效
  - 具名锚点 + 编号并存 —— 人读留编号，机器引用只认锚点，根治插入铁律导致的全盘漂移
  - L2 彻底外置 ops/l2.md，主文只留指针 —— 与 line-count.md「单一定义处」先例一致
  - 本次按实现代码待遇全走 L2+L3 —— 依 line-count.md:35「进入执行链」判据，规则文本会被模型执行
  - 所有跨文件引用一律改锚点（含现正确的）—— 机制留半截等于没建

## 📜 事件流（只追加，绝不改写历史）
- [08-03 09:05] 取基线快照 `~/.claude-band-snapshots/claudemd-fix-20260803-090524`（cp -a，39 文件，~/.claude 非 git 仓库）
- [08-03 09:0x] 全量核对跨文件引用 —— 逮出 **17 处**编号漂移，分布 6 个文件（初次口头审阅只报了 5 处，机械 grep 后翻倍）；已核验 line-count/journal/journal-templates 的铁律 3/4 引用正确、不动
- [08-03 09:1x] 用户裁定四项（范围/角色/锚点/外置度）+ 审查漏斗按实现代码待遇 —— 见上方"已定裁决"
- [08-03 09:2x] spec 落盘 `docs/superpowers/specs/2026-08-03-claudemd-maintainability-design.md`，含 17 处错误清单、12 锚点表、7 条可证伪验收标准
- [08-03 09:3x] Plan-Gate 方向关启动（图化版 `plan-gate-direction`，Run ID `wf_530acdd5-4c3`，gateDir `~/.claude/.plangate/20260803-claudemd-fix`）；3 视角互盲并发、gpt-5.6-sol + ultra + read-only
- [08-03 09:3x] 等待期复核原文，**逮出 spec 未覆盖的两处**（待与方向关意见一并处置）：① CLAUDE.md:5 第 0 铁律豁免边界写"仅适用于 **Opus 主循环**"——同属角色耦合，须纳入 C 项替换范围；② CLAUDE.md:127 §五指针不准（称 L2 调用形式在 §四 与 line-count.md，实际在 §二·4 的 `bin/l2`）——外置为 ops/l2.md 后自然解决
- [08-03 09:4x] 锚点插入点定位完毕（供派活简报用）：铁律 1–12 依次在 CLAUDE.md 第 56/57/58/70/71/78/79/80/82/84/86/87 行；§二 流水线的 1–7 是步骤编号非铁律，不加锚点
- [08-03 09:4x] 方向关 v1 完成（8 行，末行哨兵正确）、v2 完成，v3 仍在跑；**按 ops/plan-gate.md:45 硬规则须三份全读才分诊**，不因重合跳过
- [08-03 09:5x] **Plan-Gate 收口 r1：FATAL_FOUND**（v1 ✓2条 / v2 ✓0条 / v3 ✓10条，合计 12 条）。三视角全读确认。分诊：采纳 8、部分采纳 4、完全误报 0。留痕 `.plangate/20260803-claudemd-fix/disposition.md`
  - v3 再次贡献独有致命项（改名≠能力解耦、语义零变更不可证伪、回滚被高估、16/17 计数矛盾），**第二次印证不得因前两视角重合而跳过 v3**
  - 最有价值的三条：① v3-6 角色解耦只做了改名没做能力解耦 → 加 L0 三项最低能力 + fail-closed；② v3-7 "语义零变更"靠 L3 主观判断不可证伪 → 改白名单式 (a)-(e) + 机械校验；③ v3-10 计数口径混用行数与引用数 → 重新统计得 21 处（原报 16）
  - 自我修正：v1-1/v3-1 逮出我把"并存"落实成跨文件纯锚点却未定义清楚，且与验收标准 2 自相矛盾 —— 方向无误但表述确有真缺陷
- [08-03 09:5x] spec 改 v2（307 行）：新增 §三·A0 并存定义表、B2 旧→新十项映射表、B3 bin/l2 接口证据表、C2 L0 最低能力、验收改白名单式 + 机械校验 + 恢复演练 + 自举口径。撤回"两种调用形式等价"断言，降级为登记歧义
- [08-03 09:5x] 方向关 r2 启动（Run ID `wf_e59ad8a6-daf`，gateDir `.plangate/20260803-claudemd-fix-r2`）——铁律 [PLAN-GATE] 要求 spec 实质变化后必须重审
- [08-03 10:0x] **Plan-Gate 收口 r2：FATAL_FOUND**（v1 ✓5 / v2 ✓4 / v3 ✓12，合计 21 条）。留痕 `.plangate/…-r2/disposition.md`
  - **性质与 r1 相反**：r1 是"写得不够严"，r2 是**"写得太多、越过用户裁定边界"**。三互盲视角独立锁定同一批越界项（L0 能力准入制度、V-1 豁免语义、扫描论域扩到全树、"取最强版"合并）
  - **v2 视角（过度设计）从 r1 的 0 条变 r2 的 4 条** —— 我修 r1 意见时用"加规则"回应每一条，反而制造过度设计。**教训：修审查意见时的默认动作不该是加条款**
  - 三项争议上交用户裁定（D1 并存定义 / D2 铁律8附则去留 / D3 超范围两项）→ 用户裁定：确认我的并存解释、附则留主文、超范围全撤回
- [08-03 10:1x] spec 改 v3（307→262 行，**净减法**）：撤回 L0 能力制度、V-1 豁免、bin/l2 冒烟、.sh 扩域、"取最强版"；改写入正向白名单；新增三元组表/逐项核销/强制加载指针/compare-and-restore/哈希绑定
- [08-03 10:2x] **Plan-Gate 收口 r3：FATAL_FOUND**（v1 ✓4 / v2 ✓5 / v3 ✓8，合计 17 条）。留痕 `.plangate/…-r3/disposition.md`
  - **三视角一致锁定三处硬矛盾**（最强信号，互盲下独立指向同一批）：① §二·4 的 bin/l2 归属有三种互斥解释 ② §四搬走后 "Opus 两处例外" 分布矛盾 ③ 白名单"9 文件"vs 验收"10 路径"
  - **v1-3 逮出真 bug：计数 23 是错的**。我用 python 逐行求和实测 = **21**（裸条目号已含在内，被我重复加了一次）。`[EVIDENCE-FIRST]` 的正面案例——机器一算就清楚
  - **模式教训（写入日志备查）**：r1"不够严"→r2"太多"→r3"自相矛盾"，每轮修复引入下一轮问题类型。r3 的矛盾几乎全产生于"v2 删了 A 段但忘同步依赖 A 的 B 段验收"。**减法比加法更需要全文一致性检查**
  - 用户裁定 D4（指针改纯无语义，三词速记全删）/ D5（回滚与自举验收保留完整强度）
- [08-03 10:3x] spec 改 v4（349 行）：三处硬矛盾各给唯一落点；计数改 21 并取消手工总数；指针五处逐字一致且无操作语义；fail-closed 边界写明（只阻断 L2 不阻断流水线）；验收 10 加文件类型/权限并**显式声明不提供原子 CAS**（不假装解决 TOCTOU）；验收 11 改编排层记字节数+sha256（不靠模型回显）；新增 A4b 确立账本派生关系
- [08-03 10:3x] **交付前自查逮出自己的残留**：A4 里仍写"23 处"与新改的 21 冲突 —— 正是 r3 教训所指的"改一处忘同步"，已修。其余四处 grep 命中经 `rtk proxy` 原始输出核对，均为描述历史修订的正当表述
- [08-03 10:4x] 方向关 r4 启动（Run ID `wf_a20ba4dd-b63`）

- [08-03 22:xx] 用户裁定 **D8：换实证法**（方向关 4 轮 64 条不收敛，与 memory `innovation-hunt-state-v7` 同模式）→ 停文本审，写 IMPLEMENTATION-GUIDE 直接派活
- [08-03 22:xx] **派活**：codex luna 做任务 A+B（12 锚点 + 21 处引用改锚点）；隔离副本 `~/.claude-work-claudemd-fix`
- [08-03 22:xx] **交付**：A+B 完成，L0 核验逮出 1 处语义错（:52 机械按旧编号 8→ENGINE-ASSIGNMENT，实应 PR-GATE），已修；反引号格式统一 45 处
- [08-03 22:xx] Sonnet 子代理两次 API 中断（超时 / 服务端错误），第二次已进行到 3/5；**L0 接手完成任务 C+D**（依 `[DELEGATION-BAND]` 委派不可行时例外）
- [08-03 22:xx] **L2 混合作者两轮**：Codex 侧 4 条 must-fix（js 遗漏 Opus / plan-gate 旧指针 / l2.md 缩进被渲染成代码块；1 条误报）；Claude 侧 2 条 must-fix（**搬迁丢失手工调用命令**——真实信息损失、已补回；Opus 分布预判有误——实证修正 GUIDE）
- [08-03 22:xx] **L3 终检 7 轮共 36 条**，逐轮修复。最有价值的三条：
  - r1 逮出 `[FABLE-ADVISOR]` 锚点指错（Fable 语境却引 PLAN-GATE）——**正是本次要根治的病症本身**，机械替换把错误编号忠实翻译成了错误锚点
  - r4/r5 逮出 **plan-gate-direction.js 两处既有 fail-open**：schema 漏 `integrityOk` 必填（缺失即 undefined 放行）、`hasFatal=false` 但 findings 非空可带致命项进拆解关 → 均已修（闸门 2 改 `!== true`，新增闸门 3 自洽校验）
  - r6 声称 `ultra` 是非法 effort 会让 Plan-Gate 永久失败 → **实证判误报**：本次 12 次 ultra 调用全成功 + 直接实测退出码 0。审查者以先验知识覆盖实际契约（`[PRIMARY-SOURCE]` 警告的那类错）
- [08-03 22:xx] 用户裁定 **删除 L0=Codex 例外条款**（为不成立的假设性场景写规则成本无上限，条款自身滚成新矛盾源），只留一句诚实边界；两条既有系统缺陷（图化版不校验审查证据、gateDir 秒级碰撞）**登记待办另立项**
- [08-03 23:0x] 用户裁定**收尾不再跑 L3**（r6-r7 边际收益归零）
- [08-03 23:0x] **交付**：净新增 101 行（A 档）；同步前零漂移校验通过、回滚清单落盘（`~/.claude-band-snapshots/claudemd-fix-20260803-MANIFEST.txt`）；10 文件同步后哈希 10/10 一致；实文件验收 7/7 过；隔离副本已清理
