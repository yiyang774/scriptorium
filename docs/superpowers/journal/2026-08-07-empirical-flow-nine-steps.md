# 任务日志：实证流九步定案

## 状态快照（每次覆写）

- **进行到**：✅ **已交付**（L3 第六轮 GO，最终验收全绿）
- **下一步**：无。待下次遇到可拆多候选的任务时首次实跑本流程
- **未决问题**：无
- **关键路径文件**：
  - `~/.claude/ops/empirical-flow.md`（154 → 396 行，重写为九步）
  - `~/.claude/CLAUDE.md`（§1角色表 / §2头部 / §2.2 / 铁律6 / 铁律10 / 铁律11 / §4 / §6 / §9）
  - `~/.claude/ops/plan-gate.md`、`ops/journal.md`、`ops/l2.md`、`ops/journal-templates.md`
- **已定裁决**：
  - 实证流产出**仍须过 Plan-Gate**，后置为第 ⑧ 步（用户裁定）
  - ⑥A 保留 sol ultra 提方案能力，但输出契约为「证据账本→方案集→条件化判断」三段式
  - 加 ⑥B（Claude 全新上下文、只读、与 ⑥A 互盲），取消 fable5 常规评审
  - Gate 前派活窄例外**两类**：① 共享测量代码 + ③ 候选原型；TDD 例外**只给 ③**
  - **⑧ 自己提出未测新机制也须回 ①**（L3 r4 发现的绕过路径）
  - 验收采用**通读制**，不写检查器（L0 裁定：写检查器成本超过被检查对象）

## 事件流（只追加）

### 2026-08-07 · 用户裁定：实证流产出仍须过 Plan-Gate
用户原话："目前的新方案的产出还是需要过Plan-Gate，不能仅仅听codex 5.6 sol ultra"。
据此把 Plan-Gate 从"被实证流替代"改为"后置到数据之后"，流程七步 → 九步。

### 2026-08-07 · 用户提出结构疑虑
用户原话："问题就在于codex提出方法最终Plan-Gate还是codex审核，有点问题"。
L0 首版处置：把 ⑥ 降为"只挑刺不给方案"。

### 2026-08-07 · 用户裁定：撤回上述降级
用户原话："主要是我发现codex sol ultra给出的方案很好哎"。
判定 L0 首版属**过度纠正**——为堵结构疑虑砍掉流水线最值钱的一环。
改为保留 ⑥ 的方案能力但要求给 2–3 个竞争方案，风险改由 ⑧ 的"喂全部数据 + 强制唱反调"承接。
理由记录：真实风险是**盲点相关**（同家族同套先验），不是自我背书
（⑥⑧ 两次独立调用、无共享记忆、中间隔着 L0 的独立判断）。
本机只有 Claude 与 Codex 两家，无第三家可请，故该风险**不可根除只能压低**，文档须如实写明。

### 2026-08-07 · 用户批准方案
用户原话："目前这样我感觉是最好的了"。
**判定为批准方案、不构成 Plan-Gate 豁免**（依 CLAUDE.md §2.2："照此实现"这类话批的是方案不是关卡）。
故照常补跑 Plan-Gate。

### 2026-08-07 · 建档与 spec/plan 落盘
触发条件②（进入 Plan-Gate）+ ④（已产出 spec/plan 且决定据其落地）。
spec 与 plan 已写入 specs/ 与 plans/，同 slug 对齐。

### 2026-08-07 · L0 裁定：Plan-Gate 五轮后收口，砍掉验收脚本
用户原话："目前啥问题啊，修不完，奇奇怪怪的，看起来也没啥大问题"。

**判定 Gate 已进入收益递减，依 §2.2「同一争议最多两轮，两轮后由 L0 裁定并留痕」+
`[OWNERSHIP]`「不被 Codex 的持续异议无限否决」收口。**

分轮性质：
- R1–R3：真设计错（plan 预写自我豁免、⑨ 循环依赖、改动范围未闭合、T5 时序倒置）→ 全部采纳
- R4–R5：多为 L0 改版留下的编号残骸 + **验收脚本自身的工程质量**
  → 设计类采纳（spike 窄例外、容差公式前移到①、原型快审五条定义、⑨第6步限定重跑4–5）
  → **工程细节类驳回**

**裁定内容**：
1. **删除 verify.sh / verify-selftest.sh**。理由：被检查对象是四个 markdown 文件，
   `[DELEGATION-BAND]` 对非代码产出的上界判据是「L0 能否逐单元通读」，不是「必须有机器检查」。
   R5 逐项拆穿的 9 条假通过断言随之消失——**断言本身不该存在**。
   写检查器的成本已超过被检查的东西，属 `[PLAIN-LANGUAGE]`/YAGNI 同源问题。
2. 验收改为：L0 逐文件通读 + 三条 hash 断言（铁律12/§9 未改）+ L3 终检兜底。
3. R5 的三条 hash 发现仍采纳（grep -n 含行号会误报 → 用 grep -h；路径检查漏 CLAUDE.md）,
   但以「通读时注意」形式保留，不再写成脚本。

**L0 的自我批评**：规则里本就有两轮上限与误报阀门，是我没用。
一路追着审查意见往下改，把「审查是输入」做成了「审查说了算」——违 `[OWNERSHIP]`。

### 2026-08-07 · L3 终检六轮：NO-GO ×5 → GO
证据：`.plangate/empflow9-20260807/gate/l3-out.txt` 及 `l3-out-r2..r6.txt`

| 轮次 | 阻断 | 高 | 中 | 本轮确认修复 |
|---|---|---|---|---|
| r1 | 5 | 2 | — | — |
| r2 | 4 | 2 | 2 | 3 |
| r3 | 2 | 2 | 1 | 3 |
| r4 | 2 | 1 | 1 | 3 |
| r5 | 1 | 0 | 2 | 3 |
| r6 | **0** | **0** | **0** | **GO** |

**L3 逮到而五轮 Plan-Gate 全部漏掉的问题类型**（印证"审成品 vs 审文本"的差别）：
1. 跨文件不一致——改了主条款却漏了引用它的地方，**同一毛病犯了三次**
   （铁律11 → §1角色表/l2.md → 两处留痕时点）
2. **⑧ 自己提未测新机制却无回边**（r4 新发现）：⑧提新机制→写进spec→重过⑧→直接进⑨，
   一个从未预注册/原型/实测的机制成为交付依据。给 ⑥⑦ 都补了回边，唯独漏了 ⑧ 自己
3. **共享测量代码的死路**（r3 新发现）：① 要求 ③ 之前就有测量代码，
   但窄例外只覆盖"候选原型"→ 派 L1 违反 Plan-Gate、L0 亲写又违反委派纪律
4. **我的修复自己引入的问题**：
   - r2 加的"必须附 metrics 指标行"与"跑挂了也是数据"打架 → 失败候选永远满足不了门槛
   - r3 给 invalid 分支写的证据 = ④快审记录，但 ④ 报告是**二手**（违铁律4）
   - 我断言"l2.md 对原型/spike 命中0"，**因为我自己刚改了 l2.md 而失真**

**教训**：引用别的文件的状态作为断言时，改完那个文件要回头看断言还成不成立。

### 2026-08-07 · 交付验收（全绿）
- guard-selftest 退出码 0
- 铁律12 hash 与基线一致（一字未改）
- 六文件代码围栏全部配对
- 引用路径全部存在（`.guard-off` 是急停开关、`plan-gate-direction.js` 是退役登记，均为预期不存在）
- 行数：empirical-flow 154→396、CLAUDE.md 216→224、另四个 ops 文件小幅增补
