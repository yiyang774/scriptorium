# CLAUDE.md 可维护性修复 · 实施计划（plan）

日期：2026-08-03
spec：`~/.claude/docs/superpowers/specs/2026-08-03-claudemd-maintainability-design.md`（**v4**）
基线快照：`~/.claude-band-snapshots/claudemd-fix-20260803-090524`（cp -a，39 文件）
状态：待 Plan-Gate 拆解关（spec v4 正过方向关 r4）

> **与 spec v4 对齐要点**：
> - **撤回**（v3）：U2 不含"L0 三项最低能力"；U4 不含 V-1 豁免语义与 bin/l2 冒烟验收
> - **收紧**（v3）：写入限白名单 **9 个既有文件 + 1 个新增 = 10 路径**；不碰 `*.sh`/`.plangate/`/`docs/`
> - **唯一落点**（v4，消除三处硬矛盾）：§二·4 的 `bin/l2` 表述**随基线搬入 ops/l2.md**；
>   主文五处一律**纯无语义指针**（逐字一致，无"跨家族/便宜档/fail 不阻断"字样）；
>   §四 :92 的 "Opus" 例外**随正文迁往 ops/l2.md**，CLAUDE.md 只剩铁律 8 一处
> - **计数**（v4）：引用核销 **21 处**（非 23——裸条目号已含在内）+ 形式迁移表全部行

> 本计划把 spec 拆成 **5 个可独立验收的单元**。每单元自带验收标准与可跑命令。
> 拆分依据：按**文件归属与依赖方向**切，不按行数切（守 `[DELEGATION-BAND]`
> "绝不为凑行数把一个单元腰斩"）。

---

## 依赖关系（拓扑序，不得乱序）

```
U1（建立锚点定义）
 └─→ U2（CLAUDE.md 内部引用改锚点 + 角色解耦）   ← U2 依赖 U1 的锚点已存在
      └─→ U3（ops/ 跨文件引用改锚点）             ← U3 依赖 U1 定义、与 U2 无冲突文件
           └─→ U4（L2 外置为 ops/l2.md）          ← U4 改 CLAUDE.md，须在 U2 之后避免冲突
                └─→ U5（workflows/ + agents/ 引用改锚点）
```

**关键约束**：U2 与 U4 都改 `CLAUDE.md`，**必须串行**，不得并发
（`ops/line-count.md:18`「同一工作区不并发派多个写代码子代理」）。
U3 与 U5 改不同文件，理论可并行，但为口径统一仍按序执行。

---

## U1 · 建立锚点定义（CLAUDE.md §三）

**改什么**
1. 在 §三 标题下、铁律 1 之前插入 spec §三·A2 的"引用规则"说明段（含不得复用/弃用策略）。
2. 为 12 条铁律标题各追加锚点，插入位置＝标题加粗句号之后、正文之前。
   精确行号（改动会移位，须**从后往前改**）：
   | 铁律 | 行 | 锚点 | 铁律 | 行 | 锚点 |
   |---|---|---|---|---|---|
   | 1 | 56 | `[OWNERSHIP]` | 7 | 79 | `[DIAGNOSE-FAILURE]` |
   | 2 | 57 | `[SELF-CONTAINED-BRIEF]` | 8 | 80 | `[ENGINE-ASSIGNMENT]` |
   | 3 | 58 | `[DELEGATION-BAND]` | 9 | 82 | `[PR-GATE]` |
   | 4 | 70 | `[EVIDENCE-FIRST]` | 10 | 84 | `[SKILL-PIPELINE]` |
   | 5 | 71 | `[PRIMARY-SOURCE]` | 11 | 86 | `[PLAN-GATE]` |
   | 6 | 78 | `[GRANT-PERMISSIONS]` | 12 | 87 | `[FABLE-ADVISOR]` |

**不许动**：任何铁律的正文语义；§二 流水线的步骤编号 1-7（非铁律，不加锚点）。

**验收**
- 12 个锚点各恰好在铁律标题行出现 1 次（逐个 `grep -c`）
- **三元组核对**（spec v3 §二·A2 / 验收 4）：逐行确认「编号 N — 标题关键词 — 锚点」三者匹配，
  防"锚点合法但指错条款"（如 `[PLAN-GATE]` 与 `[SKILL-PIPELINE]` 互换仍能通过唯一性检查）
- `diff` 中除追加锚点与新增说明段外，§三 无其他改动

---

## U2 · CLAUDE.md 内部引用改锚点 + 角色解耦

**改什么**
1. **内部铁律引用改锚点**：CLAUDE.md 内全部 `铁律 N` 引用 → `` `[ANCHOR]` ``。
   **含三处编号错误**（改锚点时一并修正，因锚点按语义指向）：
   - :23 "见 §二·2 与铁律 11" → 实指 Fable5 顾问 → `[FABLE-ADVISOR]`
   - :52 "见铁律 8" → 实指 PR 合并 → `[PR-GATE]`
   - :108 "铁律 7 / §二·5" → 实指 L3 终检 → `[ENGINE-ASSIGNMENT]`
   其余正确引用同样改锚点（全量迁移，spec §三·A3）。
2. **角色解耦**（spec §三·C1）：作为 L0 角色名的 "Opus" → "L0 主循环"。
   已知位置：:5（第 0 铁律豁免边界）、:9（"Opus 当大脑…"整句）、:17、:21（§一表格）、
   :27/:37/:51（§二流程）、§三 各铁律正文内、§四各处、§六:133。
   **例外不改**：:80 铁律 8 "永不 Opus / Fable"、:92 §四 "永不用 Opus / Fable"
   （此处指具体模型，非角色）。
3. **加 L0 定位句**（spec v3 §二·C2，**仅一句，不含能力门槛**——v2 的三项能力已按用户裁定 D3 撤回）。
4. **铁律 12 加消歧句**（spec v3 §二·C3）。

**验收**
- `grep -n 'Opus' CLAUDE.md` 的输出仅剩 :80 与 :92 两处（±锚点插入导致的行号偏移）
- `grep -nE '铁律 *[0-9]' CLAUDE.md` **仅**匹配 §三 的 12 个铁律标题行前缀（`^[0-9]\+\. \*\*`），
  其余位置 0 行 —— 含 :23/:52/:108 三处已知错误必须被修掉，**不得以"CLAUDE.md 内允许数字"为由残留**
  （spec v3 验收 3，修复 v2 的自相矛盾）
- L0 定位句为单句，**不含**任何能力门槛或 fail-closed 表述

---

## U3 · ops/ 跨文件引用改锚点

**改什么**（4 个文件，共 15 处引用）

| 文件 | 处数 | 改法 |
|---|---|---|
| `ops/fable5.md` | 8 | 标题"铁律 11 外置"→"`[FABLE-ADVISOR]` 外置"；正文条目号 `11.`→`12.`（人读编号，据 spec 表组 5）；铁律 10→`[PLAN-GATE]` ×4；铁律 5→`[GRANT-PERMISSIONS]` ×2 |
| `ops/plan-gate.md` | 4 | 铁律 10→`[PLAN-GATE]` ×2（含 :86 无空格的"铁律10"）；铁律 5→`[GRANT-PERMISSIONS]` ×2 |
| `ops/pr-merge.md` | 2 | 标题"铁律 8 外置"→"`[PR-GATE]` 外置"；正文条目号 `8.`→`9.` |
| `ops/line-count.md` `ops/journal.md` `ops/journal-templates.md` | 7 | 铁律 3→`[DELEGATION-BAND]`、铁律 4→`[EVIDENCE-FIRST]`（**编号本已正确**，仅做形式迁移） |

引用格式统一为：`` `[ANCHOR]`（CLAUDE.md §三） ``，首次出现标宿主，同文件后续可略。

**不许动**：任何 shell 脚本块内的命令、哨兵、三闸逻辑、fail-closed 语义。

**验收**
- `grep -rnE '铁律 *[0-9]' ~/.claude/ops/` 输出为 0 行
- **两处裸条目号必须显式核销**（grep 抓不到，spec v3 验收 2）：
  `ops/fable5.md:7` 的 `11.` → `12.`、`ops/pr-merge.md:6` 的 `8.` → `9.`
- `diff` 显示脚本块（bash 代码块）内容零改动——哨兵、三闸、fail-closed 语义原样
- `ops/l2.md` 尚未创建（属 U4）

---

## U4 · L2 外置为 ops/l2.md（纯搬迁）

**改什么**（据 spec v3 §二·B，权威基线制取代映射并集制）
1. **新建** `~/.claude/ops/l2.md`：**指定 CLAUDE.md §四 的 L2 大段为唯一权威基线，逐字搬入**
   （它是五处中唯一完整规范表述，含四判据全文与固定句式）。
   其余四处是摘要/流程说明，**只换指针、内容不并入**；差异逐条登记为待裁定项。
   结构：§1 定位与触发范围 / §2 跨家族选档矩阵 / §3 四判据 + fail-不阻断 + 固定句式 /
   §4 调用（`bin/l2` 首选——**照搬 §二·4 原有文字，不新增规范**）/ §5 输出契约与 must-fix 复审 /
   §6 待办登记（两种 Claude 调用形式等价性**尚未验证、本次不裁定**，**不给任何有效性豁免**）。
2. **压缩 CLAUDE.md 五处**为**纯无语义指针**（裁定 D4，五处逐字一致、无任何操作语义词）：
   > ▶ **跑 L2 前必须先 `Read ~/.claude/ops/l2.md`；读不到则不得发起 L2。**

   | 位置 | 目标状态 |
   |---|---|
   | §一表格 L2 行 | 表格字段保留，"负责什么"格改为该指针 |
   | §二·4 | 整条压成该指针（`bin/l2` 表述**随基线搬入 ops/l2.md §4**） |
   | §四 L2 大段 | **整段删除**（逐字搬入 ops/l2.md），原位留该指针 |
   | §五速记 L2 行 | 删除 L2 部分（含修正 :127 错误指针），改该指针 |
   | 铁律 8 附则 | **保留硬门槛句**（D2）+ 其后附同一指针 |

   **fail-closed 边界**：读不到**只阻断 L2 这一步、不阻断流水线**（L2 本就 fail-不阻断），
   按现行语义记"L2 未生效"进 L3；但**不得凭记忆发起**（凭记忆＝参数/语义可能不符＝无效发起）。
3. **保留**（裁定 D2）：铁律 8 附则"L2 不得作为跳过 L3/Plan-Gate 的理由"**留在 CLAUDE.md**。
4. **`ops/l2.md` 首行**注明配置根解析规则（若不在 `~/.claude` 则按实际根路径解析）。

**验收**
- `ops/l2.md` 存在，含"有效发起"四判据全文（四条齐全）
- CLAUDE.md 中四判据完整表述出现 0 次；**五处指针逐字一致**（`diff` 比对文本完全相同），
  且五处**均不含**"跨家族/便宜档/fail 不阻断"等操作语义词（裁定 D4）
- **逐字比对**：l2.md 的搬迁段落与基线快照中 CLAUDE.md §四 对应段落语义一致（非并集、非收紧）
- §6 待办措辞含"尚未验证""本次不裁定"，且**不含**任何"不因判据③失效"式豁免
- 铁律 8 附则仍在 CLAUDE.md；**"Opus" 在 CLAUDE.md 恰好 1 处、在 ops/l2.md 恰好 1 处**
- CLAUDE.md 净减少 55-85 行

---

## U5 · workflows/ + agents/ 引用改锚点

**改什么**
- `workflows/plan-gate-direction.js`：:17 铁律 7→`[ENGINE-ASSIGNMENT]`；
  :101 铁律 7→`[ENGINE-ASSIGNMENT]`、铁律 5→`[GRANT-PERMISSIONS]`。
  :4 与 :178 的"铁律 11"编号正确 → 改 `[PLAN-GATE]`（形式迁移）。
  **只改注释与 prompt 字符串内的引用文字，不改任何逻辑、schema、命令参数。**
- `agents/fable-readonly-advisor.md`：frontmatter description 内"铁律 11"→`[FABLE-ADVISOR]`。
  **不改 `tools:` 字段**。

**验收**
- `node --check ~/.claude/workflows/plan-gate-direction.js` 退出码 0
- `agents/fable-readonly-advisor.md` frontmatter 可解析，`tools:` 值与基线逐字相同
- `grep -rnE '铁律 *[0-9]' ~/.claude/workflows/ ~/.claude/agents/` 输出 0 行

---

## 交付后（Opus 收口，不派活）

1. **机械复核行数**：`git diff --no-index --numstat <快照> <现状>`，rc 只容忍 0/1，汇总判档
2. **受影响路径清单**：每路径记 `基线sha256 → 目标sha256`（新增记 `ABSENT`）
   **+ 文件类型与权限位**（`stat -f '%HT %Lp'`）——内容哈希不覆盖 symlink/权限（spec 验收 10）
3. **恢复演练**：隔离临时目录完整还原并 diff 验证；新增文件回滚须显式 `rm`（rm 前校验哈希）。
   **已知边界**：本方案无原子 CAS，依赖"回滚期间无其他写者"前提（本机单人单会话成立）
4. **机械校验**（spec 验收 7）：`node --check` / frontmatter `tools:` 未变 / 代码块闭合偶数
5. **L2 跨家族快审**：作者家族取决于派谁；按**旧规则**发起——
   把基线快照的旧 `CLAUDE.md` + `review.diff` 打成**不可变输入包**，
   由 L0 记录**实际送入 stdin 的字节数与 sha256**（不要求模型回显，spec 验收 11）
6. **L3 终检**：`codex exec -m gpt-5.6-sol -c model_reasoning_effort="xhigh" -s read-only`，
   显式要求按 spec 验收 6 的白名单 (a)-(e) 逐条核对越界改动
7. **最终态重审**：若 L2/L3 后又有改动，对冻结态重新发起（memory「冻结态终审收尾」）

---

## 派活分配（待拆解关通过后定档）

| 单元 | 预估实现净新增 | 建议档 | 理由 |
|---|---|---|---|
| U1 | +20～30 | codex luna 或 Opus 直接 | 机械追加，风险低 |
| U2 | +15～25 | **Sonnet 5** | 需理解"角色 vs 模型"语义区分，强耦合判断 |
| U3 | ±0～10 | codex luna | 纯机械替换，有精确清单 |
| U4 | +100～150 | **Sonnet 5** | 搬迁需按映射表逐项核，需深度理解 |
| U5 | ±0～5 | codex luna | 机械，但须保 js 语法 |
| **合计** | **+135～220** | | 落 ≤400 **A 档** |

**混合作者预警**：若 U1-U5 分派给不同家族，按 `ops/line-count.md:30`
「混合作者的 L2 选档 → 两个家族各审一轮完整变更集」。
