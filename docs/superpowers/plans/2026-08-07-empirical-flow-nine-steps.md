# plan：实证流九步定案 · 落地拆解 v3

- spec：`~/.claude/docs/superpowers/specs/2026-08-07-empirical-flow-nine-steps.md`（v3）
- slug：`empirical-flow-nine-steps`
- **v1 → v2**：删除预写的自我豁免预案（r1 ③-2）；改动范围从「CLAUDE.md 两处」扩至
  铁律10、铁律11 全条、`ops/plan-gate.md`（r1 ④-1）
- **v2 → v3**（R3 不通过，3 条致命）：**修正时序倒置**（Gate 后补票 → Gate 先行，r3 ④-2）；
  **改动范围闭合**（加 T3b 改 `ops/journal.md`，r3 ④-1）；验收改为完整命令 + 失败条件（r3 ④-4）
- **改动文件共四个**：`ops/empirical-flow.md`、`CLAUDE.md`、`ops/plan-gate.md`、`ops/journal.md`
  （`ops/l2.md` 不改 —— 采用 spec §2④ 方案 B，在实证流内定义「原型快审」并声明不是 L2）

---

## 任务拆解

### T1 · 重写 `~/.claude/ops/empirical-flow.md`（主体）

**做什么**：七步 → **九步**（⑥A/⑥B 是 ⑥ 的两个并行分支，不单独计步），按 spec §2 逐步写死。

**必须逐字采用的三段成文措辞**（来自 Plan-Gate 第二轮，不得改写要点）：
- ⑥ 的输出契约（证据账本 → 方案集 → 条件化判断）—— `gate/out-r2.txt` Q2 代码块
- 入口判据四条 + 核心反证 —— `gate/out-r2.txt` Q4 代码块
- 「模型独立性与已知盲点」整段 —— `gate/out-r2.txt` Q6 代码块

**保留不动**：开头动因段（四轮 Plan-Gate 对照实测）、`runs/` 布局与 `bin/exp-index` 引用。

**删除**：原 ⑦ fable5 常规评审整段；v1 提过的「可选 L2 跨家族粗筛」不得写入。

**验收**：spec §5 第 1–5、9、10 条 + 第 15 条（通读）。

**层级**：L0 直接写。理由：`[DELEGATION-BAND]` 非代码产出上界由「L0 能否逐单元通读」定；
本文件每句措辞都是两轮 Plan-Gate 的裁定结果，派 L1 需把全部裁定复述进简报，
简报会比产出还长——委派开销明显大于收益。

### T2 · 改 `~/.claude/CLAUDE.md`（三处，非两处）

**T2a · §2 头部分流说明**：补成「两条路都过 Plan-Gate 与 L3，**区别只是 Plan-Gate 的时点**：
标准流水线在派活前，实证流在数据出来之后」。

**T2b · 铁律11 `[PLAN-GATE]` 全条重写**（不是只改首句 —— r1 ④-1）。需一并处理的表述：
- 标题「**第一步必过** Plan-Gate」
- 「在派活（**第 3 步**）前必过」
- 「方可进**第 3 步**」
- 结尾「**派活前必过** Plan-Gate 是硬门槛」
改为覆盖两条路的时点表述，判据仍是「产出是否会成为派活执行的依据」。

**T2c · 铁律10 `[SKILL-PIPELINE]` 加 spike 例外**：现文要求任何写代码任务走 TDD、无例外，
与 spec §2③ 冲突。加：原型/spike 阶段豁免 TDD，但**晋级须走 spec §2⑨ 的六步有序协议**，
且**不得声称原型「补做后就变成了 TDD」**。例外边界写窄，防扩大解释。

**不改**：铁律12、§4 的 fable5 文字；§9 防绕过标记。（验收第 6、7 条用 hash 证明）

### T3 · 改 `~/.claude/ops/plan-gate.md`（v1 曾禁改，现解除 —— r1 ④-1）

该文件三处把 Plan-Gate 写成「派活前必过」，是唯一事实源，不改则与铁律11 矛盾。
只改时点表述，**不动命令、三闸脚本、四维定义、威胁模型段**。

### T3b · 改 `~/.claude/ops/journal.md`（v2 漏项 —— r3 ④-1 致命）

现行「何时建日志」是**正面白名单**（四条触发条件），**不会自然覆盖**「进入实证流入口判定」
这个时点，而 spec §4 要求入口判定与决策表必须在原型实现前写入 journal。

**做什么**：在建档触发条件中增加一条「⑤ 判定进入实证流（须同时记入口判据逐条结论 + 决策表）」，
并在「六类必记事件」中增加「实证流入口判定」。**不动**其余字段、模板、错误档小节。

### T4 · 核验

按 spec §5 执行：

1. **机械 11 条**（§5.1）：逐条跑完整命令，**贴出真实输出**（不得只写「通过」）
2. **通读 6 条**（§5.2 第 12–17）：L0 逐条写明判定结论与依据
3. hash 基线在 `.plangate/empflow9-20260807/baseline/`（含 `tielv12.txt`、`sec9.txt`、
   `fable-mentions.txt`、汇总 `HASHES.txt`）—— **spec 与 plan 引用同一路径口径**
4. 第 8 条全域扫描的结果须**逐项**记录「已改 / 保留及理由」，不得只报总数

### T5 · L3 终检（落地后，硬门槛）

- `codex exec -m gpt-5.6-sol -c model_reasoning_effort="xhigh" -s read-only`
- **审查材料（r1 ④-4：范围必须扩大）**：最终态 `ops/empirical-flow.md` **全文** +
  **完整 CLAUDE.md** + `ops/plan-gate.md` + `ops/l2.md` + `ops/journal.md`
- 简报必带【环境与威胁模型】段
- 重点问：九步之间有无逻辑断点？跨文件引用是否已全部一致（尤其 Plan-Gate 时点、TDD 例外）？
  §3 的盲点段落有无夸大？新加的例外会不会被未来会话扩大解释？
- 改完按 §2.6 对**最终态**再跑一次

## 依赖顺序（v3 已修正时序倒置）

> **v2 的错误（r3 ④-2，致命）**：v2 写成「T1–T3 实施 → T4 核验 → T5 重过 Gate」，
> 即**规范先改、Gate 后补票**——与「派活前必过」的整个立意相反。
> 而当前这一轮 Gate 恰恰就是 v3 派活前的那一次。

```
【当前位置】v3 过 Plan-Gate  ──通过──▶ T1/T2/T3/T3b 并行实施 ──▶ T4 核验 ──▶ T5 落地后 L3 终检
                  │
                  └─不通过─▶ 修订 spec/plan ──▶ 重过 Gate（回到起点）
```

**只有当实施过程本身导致 spec/plan 再次实质变化时**，才需要重新过 Plan-Gate；
仅按既定 spec 落笔不触发重审。**T5 之前不得声称完成。**

## 风险与预案

| 风险 | 预案 |
|---|---|
| 改铁律11 时误伤其他锚点引用 | T4 第 1 步逐条 grep；第 6、7 条 hash 比对专为此设 |
| 铁律10 的 spike 例外被未来会话扩大解释 | 例外边界四条写死（spec §2③）+ 晋级协议同处；T4 通读第 13 条专查 |
| 文档写长后自身违反 `[PLAIN-LANGUAGE]` | T4 通读第 17 条自查 |
| 文档改动引入围栏不配平 | T4 第 9 条**逐文件**检查（总数为偶数不充分 —— r1 ④-5） |

> **注（v1 删除项）**：v1 曾在此表写「若 L3 报出盲点相关未解决 → 判威胁模型错配放行」。
> 该条已删除，**不留替代**。理由：那是提前给未出现的意见判误报，属自我豁免，
> 且与安全威胁模型无关。L3 的任何意见一律按证据正常分诊。
